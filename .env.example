# Supabase — the anon/publishable key is safe to expose in the browser; row-level
# security (not key secrecy) protects the data. Set these in Vercel (Project →
# Settings → Environment Variables). If unset, the app falls back to the existing
# project's public values (see src/lib/supabase.ts) so it runs with zero config.
NEXT_PUBLIC_SUPABASE_URL=https://tioeqxdulxqiptlszldp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
